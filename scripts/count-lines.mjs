import {execFileSync} from 'node:child_process';
const files=execFileSync('git',['ls-files','-z'],{encoding:'utf8'}).split('\0').filter(Boolean),rows={Source:{total:0,nonblank:0},Tests:{total:0,nonblank:0},Styles:{total:0,nonblank:0},Documentation:{total:0,nonblank:0},Configuration:{total:0,nonblank:0}};let excluded=0;
for(const file of files){if(file==='package-lock.json'||file.startsWith('components/ui/')||/\.(png|jpg|woff2|ico|zip|gz|pbf)$/.test(file)){excluded++;continue;}const text=execFileSync('git',['show','HEAD:'+file],{encoding:'utf8'});const lines=text.replace(/\r\n/g,'\n').replace(/\n$/,'').split('\n');const category=/test|fixture/.test(file)?'Tests':/\.css$/.test(file)?'Styles':/\.md$/.test(file)?'Documentation':/\.(tsx?|mjs|py|ps1|bat)$/.test(file)?'Source':'Configuration';rows[category].total+=lines.length;rows[category].nonblank+=lines.filter(x=>x.trim()).length;}
console.log('| Category | Total lines | Nonblank lines |\n|---|---:|---:|');for(const[k,v]of Object.entries(rows))console.log(`| ${k} | ${v.total} | ${v.nonblank} |`);console.log(`\nExcluded ${excluded} lockfile, generated scaffold-component or binary files. Counts use tracked files at HEAD. Command: node scripts/count-lines.mjs.`);

services:
  regional-map:
    build: .
    restart: unless-stopped
    ports:
      - "8787:8787"
    environment:
      MBTILES: /srv/maps/data/ontario.mbtiles
      PLACE_INDEX: /srv/maps/data/places.sqlite3
    volumes:
      - ./data:/srv/maps/data:ro
